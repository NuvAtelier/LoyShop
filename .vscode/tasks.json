{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and Package",
      "type": "shell",
      "command": "mvn clean package",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}

